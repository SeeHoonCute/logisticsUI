 import { createSelector } from "reselect";

// import { rootReducerType } from "../rootReducer";

// const getPending = (state: rootReducerType) => state.auth.pending;

// const getToken = (state: rootReducerType) => state.auth.token;

// const getError = (state: rootReducerType) => state.auth.error;

// export const getAuthSelector = createSelector(getToken, (token) => token);

// export const getPendingSelector = createSelector(
//   getPending,
//   (pending) => pending
// );

// export const getErrorSelector = createSelector(getError, (error) => error);